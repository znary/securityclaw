{
  "id": "safeclaw",
  "name": "SafeClaw Security",
  "description": "Runtime policy enforcement, transcript sanitization, and audit events for OpenClaw.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "configPath": {
        "type": "string",
        "description": "Path to the SafeClaw YAML policy file, relative to the plugin root or absolute."
      },
      "overridePath": {
        "type": "string",
        "description": "Optional JSON override file for runtime strategy updates from the admin panel."
      },
      "webhookUrl": {
        "type": "string",
        "description": "Optional override for SecurityDecisionEvent webhook delivery."
      },
      "policyVersion": {
        "type": "string"
      },
      "environment": {
        "type": "string"
      },
      "approvalTtlSeconds": {
        "type": "number",
        "minimum": 1
      },
      "persistMode": {
        "type": "string",
        "enum": ["strict", "compat"]
      },
      "decisionLogMaxLength": {
        "type": "number",
        "minimum": 64,
        "description": "Max serialized argument length included in SafeClaw decision logs."
      },
      "statusPath": {
        "type": "string",
        "description": "Path to runtime status JSON written by SafeClaw for dashboard monitoring."
      }
    }
  }
}
