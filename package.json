{
  "name": "safeclaw",
  "version": "0.1.0",
  "description": "SafeClaw security plugin MVP for OpenClaw-compatible hooks.",
  "type": "module",
  "private": true,
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types"
  }
}
